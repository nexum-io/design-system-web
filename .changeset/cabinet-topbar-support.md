---
"@nexum-io/design-system": patch
---

Add optional `supportLabel` / `onSupport` to `CabinetAuthSignedIn`: `CabinetTopbar` renders a Support item before Sign out in the signed-in dropdown when both are provided; consumers that omit them are unchanged.
