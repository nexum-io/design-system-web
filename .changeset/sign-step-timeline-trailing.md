---
"@nexum-io/design-system": patch
---

Add optional `trailing` slot to `SignStepTimeline` steps so consumers can render an action (e.g. Disconnect) to the right of the step label/description/result.
